# ===================================
# Filter Scope Definitions
# ===================================

scopes:
  date:
    label: 'Filtrer par date'
    type: daterange
    conditions: date >= ':after' AND date <= ':before'
  published:
    label: 'Tous / Non-publié / Publié'
    type: switch
    conditions:
      - published <> true
      - published = true
